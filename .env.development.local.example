# Copy this file to .env.local (gitignored) to point the app at a local
# Django backend instead of production. Vite only exposes variables
# prefixed with VITE_ to client-side code.

VITE_SITE_BASE_URL=http://localhost:8000
